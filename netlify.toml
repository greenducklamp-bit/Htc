[build]
  command = "npm run build"
  publish = "build"
[build]
  command = "echo const React=require('react');const ReactDOM=require('react-dom/client');const App=require('./App').default;ReactDOM.createRoot(document.getElementById('root')).render(React.createElement(App)) > src/index.js && npm run build"
  publish = "build"
